<div class="home-page">
  <h1 class="home-page__title">Select Picker Prototype</h1>
  <p class="home-page__lead">Phlex renders the shell, Stimulus opens and searches, and the users endpoint streams the options.</p>

  <%= raw Ui::UserSelectComponent.new(
    results_path: users_path,
  ).call %>
</div>
